import styled from 'styled-components'

export const Header = styled.header`

    margin-bottom: 32px;

    span {
        color: var(--gray-200);
    }
`

export const Main = styled.main`
    margin: 2rem 0 6rem;
`